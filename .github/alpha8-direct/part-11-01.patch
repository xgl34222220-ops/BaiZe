+- 受保护共享目录不扫描普通文件，但会只读检查其内部目录壳，从而发现真实空目录。
+- 新增原生“文件归类”工具，扫描所有明确命名为 Download / Downloads / 下载的目录。
 
-## 2. 空文件与空目录修复
+## 文件归类范围
 
-- 当前 NativeProfileEngine 会在 `Android/DCIM/Documents/Download` 等共享存储目录处提前剪枝，导致这些目录即使真实为空也没有 post-order 检查。
-- 改为“内容子树仍受保护，但目录本身可进行只读空状态检查”。
-- 清理前再次以 Root 权限确认：路径未变化、不是软链接、不是挂载点、列表可读取且仍为 0 项，才允许删除。
-- 自动任务默认不反复删除系统会立即重建的标准目录；手动空项目扫描会显示并标注跳过/可清理原因。
-- 扫描结果增加“未清理原因”，区分非空、不可读取、挂载点、保护目录、扫描后发生变化。
-
-## 3. 文件归类
-
-第一阶段做成独立手动工具，不加入自动清理：
-
-- 分类：图片、视频、音频、文档、安装包、压缩包、电子书、其他。
-- 扫描目标不是单一公共 `Download`，而是本机所有可识别的下载目录。
-- 覆盖共享存储中的 `Download` / `Downloads` / `下载`，以及厂商和应用自建的下载目录。
-- 覆盖 `Android/data/<包名>/files`、`Android/media/<包名>`、应用外部私有目录中的下载子目录。
-- Root 可访问时，也发现 `/data/user/<用户>/<包名>` 与 `/data/user_de/<用户>/<包名>` 内明确命名为下载用途的子目录；这些来源标记为“应用内部下载”，默认逐来源确认。
-- 不排除应用私有下载目录；但不会把整个应用私有目录都当作下载目录，只处理服务端识别出的下载子树，避免误移动数据库、配置、缓存和账号数据。
-- 先扫描并生成不可变移动计划，按来源目录分组展示；用户可关闭某个应用或某个下载来源后再确认。
-- 目标默认位于用户选择的“白泽归类”目录，各分类建立子目录。
-- 同名文件不覆盖，使用安全重命名；跨文件系统时复制、校验大小与摘要后再删除源文件。
-- 每次归类保存操作清单，支持撤销；源文件或目标目录在确认后变化时跳过。
-- 首版不自动按内容猜测，不读取私人文档正文，仅使用扩展名、MIME 与必要文件头做类型判断。
-
-## 下载目录识别边界
-
-- 目录名按大小写无关识别 `download`、`downloads`、`下载`，并支持内置的常见厂商/应用下载目录规则。
-- 每个候选来源记录所属用户、应用包名、来源类型和规范化路径。
-- 不跟随软链接，不跨越挂载点，不进入 `/proc`、`/sys`、`/dev`、`/metadata`、`/data/adb` 等系统区域。
-- 对应用内部下载目录只移动普通文件；套接字、设备节点、锁文件、数据库及正在变化的文件全部跳过。
+- `/data/media/<user>` 公共存储及厂商、应用自建目录。
+- `Android/data/<包名>` 与 `Android/media/<包名>` 内的下载子目录。
+- Root 可读取的 `/data/user/<user>/<包名>` 与 `/data/user_de/<user>/<包名>` 下载子目录。
 
 ## 安全边界
 
-- 文件归类与垃圾清理使用不同的任务、快照和历史记录。
-- 不允许 UI 提交任意源路径作为移动授权；移动目标必须来自服务端扫描计划。
