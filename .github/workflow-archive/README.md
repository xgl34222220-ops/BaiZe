# Historical workflows

These files are retained only as release/build history. GitHub Actions does not execute workflows outside `.github/workflows`; the active automation surface is limited to `ci.yml` and the manually approved `release.yml`.
