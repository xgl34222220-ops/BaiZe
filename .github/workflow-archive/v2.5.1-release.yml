name: BaiZe v2.5.1 Historical Release Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-release-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Keep v2.5.1 immutable
        run: |
          echo "BaiZe v2.5.1 is frozen as a historical formal release."
          echo "Do not move the v2.5.1 tag or overwrite its assets."
