name: Apply Dual Theme Redesign

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dual-theme-ui-redesign
          fetch-depth: 0
      - name: Paused for transport repair
        run: echo 'Manual trigger only while repair parts are verified.'
