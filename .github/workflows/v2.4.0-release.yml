name: BaiZe Release Staging Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging-guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is temporarily paused while version metadata is migrated to v2.5.0."
