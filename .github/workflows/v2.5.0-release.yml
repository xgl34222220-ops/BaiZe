name: BaiZe v2.5.0 Historical Release Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-release-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Keep v2.5.0 immutable
        run: echo "BaiZe v2.5.0 is frozen as a historical formal release. Use the v2.5.1 release workflow for current builds."
