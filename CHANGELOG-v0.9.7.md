# 白泽 v0.9.8

- 定时周期下限由 6 小时调整为 1 小时，WebUI、配置校验与服务端校验保持一致。
- 默认缓存、空文件、规则垃圾和残留碎片均设为每小时执行；深度清理仍默认关闭。
- 定时服务由每小时检查改为每分钟轻量检查：任务到期后在息屏、电量和停止条件满足时尽快执行，不再额外等待一整小时。
- 首次未初始化任务不再无条件延后一整个周期；安装脚本仍会保留升级场景的安全计时。
- 新增 `/data/adb/safesweep/scheduler.env` 记录定时状态与等待原因；各任务输出写入 `/data/adb/safesweep/logs/scheduler-<任务>.log`，便于排查。

## v0.9.8

- WebUI 增加定时服务状态卡片，直接显示已完成、等待息屏/电量、已暂停或执行失败及最后更新时间，排查不再依赖翻日志。
- 统一升级时补齐缺失配置项的默认值：空文件和隐藏垃圾均保持默认的 0 天保留，不会与新装默认设置产生差异；已保存的用户配置不受影响。
