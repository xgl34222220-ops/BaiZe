# 白泽 v1.1.0-Beta1｜性能与界面打磨版

## 扫描与清理

- 日常全量任务改为一次遍历共享存储，同时分类空文件、空目录、隐藏垃圾和残留碎片，避免同一路径被重复扫描。
- 隐藏目录单独收口处理，修正扫描结果可能重复统计目录内文件的问题。
- 保留 Android、相册、文档、下载、影音、字体等公共目录保护；下载目录只匹配明确的中断下载后缀。
- 缓存、空项目和规则垃圾可在 WebUI 中独立执行，仍可使用完整的一键清理。

## WebUI

- 重构为首页、清理、计划、记录四个页面，降低透明模糊层数量，提升长页面滚动和低端设备渲染流畅度。
- 首页使用真实 DATA 分区数据绘制占用圆环，并显示剩余、已用与总容量。
- 新增累计释放、累计耗时、普通文件、碎片、空文件、空目录和清理次数概览。
- 专项工具在窄屏上不再隐藏扫描状态，按钮和小字号信息重新排版。
- 保持五组任务 1–720 小时独立间隔，默认仍为 1 小时；每日固定时刻继续可选。

## 通知与诊断

- 记录最近一次通知投递状态、通道、原因和时间，首页与计划页可直接查看。
- 通知仍按系统 Shell、Root 长文本、Root 基础通知依次回退。
- 测试通知完成后立即刷新诊断状态。

## 开发与验证

- 状态读取减少外部命令调用。
- 增加 WebUI 元素绑定、配置范围、JavaScript 语法和打包内容检查。
