# 白泽 v1.1.0-Beta2｜轻量 WebUI 调整

本版针对 Beta1 在手机 WebView 中卡顿、信息层级过多的问题重新做减法。

## 界面

- 首页将设备、最近结果、真实存储容量和累计释放合并为一个概览区域。
- 定时与通知状态改为紧凑的双行列表，日常清理入口改为统一列表。
- 删除重复的英文小标题、装饰圆环和不必要的卡片层级。
- 使用纯色背景、单层边框和少量状态色，保持亮色与暗色模式。
- 窄屏专项工具保持两行布局，扫描结果和操作按钮不会被隐藏。

## 流畅度

- 删除固定背景渐变、毛玻璃、模糊、页面入场动画和大面积阴影。
- 清理运行期间状态轮询由 2 秒调整为 4 秒。
- 状态数据没有变化时不再重复更新全部 DOM。
- 运行秒数由前端本地更新，不再为计时频繁执行 Root 命令。

## 保持不变

- 独立定时仍支持 1–720 小时，常用任务默认 1 小时。
- Beta1 的一次共享存储扫描、通知诊断、累计统计和安全保护全部保留。
