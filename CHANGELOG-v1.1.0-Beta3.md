# 白泽 v1.1.0-Beta3｜MIUIx 玻璃界面

## MIUIx 视觉

- 使用蓝紫渐变环境、半透明渐变卡片、玻璃高光与柔和状态色。
- 底部导航改为悬浮玻璃底栏，选中块会在切页时平滑移动并改变液态轮廓。
- 顶部概览保留真实存储容量、最近结果与累计释放，同时加入受控玻璃层。
- 完整支持暗色模式，暗色玻璃使用独立透明度和边框层级。

## 流畅度预算

- 真正的实时 `backdrop-filter` 只用于顶部概览和悬浮底栏两处。
- 其他卡片用半透明渐变模拟玻璃，不为每张卡创建模糊合成层。
- 页面不使用固定背景、不使用全屏模糊，只保留一个底栏 GPU 位移层。
- 清理任务运行时自动关闭实时模糊；低核心设备和减少动态效果偏好也会自动降级。
- 状态没有变化时不重写 DOM，运行秒数继续在前端本地更新。

## 后台优化

- 状态接口启动时只解析一次配置，减少 WebUI 每次轮询产生的文件读取。
- 定时服务每轮只加载一次配置，不再为各条件重复启动 `sed` 和 `tail`。
- 定时服务状态文件由四次外部读取收敛为一次 Shell 解析。
- 独立定时继续支持 1–720 小时，常用任务缺省值保持 1 小时。

## 清理能力

- 保留 Beta1 的一次共享存储遍历、多类别分流、通知诊断、累计统计和全部安全保护。
