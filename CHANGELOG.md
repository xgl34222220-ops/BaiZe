# 更新日志

本文件由历史的 19 个 `RELEASE_NOTES_*.md` / `CHANGELOG-*.md` 合并而来。
更早的散装文件已删除，历史版本的产物与说明仍可在 GitHub Releases 页面查到。

版本号以 `module.prop` 为唯一来源，发布流程见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## v2.5.7

本版本修复正式版设置页面开关在操作后自动回退的问题，并加固设置保存与后台状态轮询之间的同步逻辑。

### 设置开关不再自动回退

- 设置页新增本地配置草稿，切换开关后会立即保留用户选择。
- Root 服务前台轮询只更新任务运行状态、队列、下次执行时间和守护进程状态，不再覆盖尚未保存的设置。
- 点击“保存设置”并由服务端确认后，界面才结束草稿状态并同步最终配置。
- 保存失败或服务端暂时返回旧配置时，当前修改会继续保留，可直接再次保存。

### 覆盖范围

- 修复“仅在息屏时执行”“仅在充电时执行”“仅在系统空闲时执行”。
- 修复“归类时等待息屏”“归类时等待充电”“归类时等待系统空闲”。
- 同时保护最低电量、单文件上限、任务完成通知和零结果通知等尚未保存的设置。

### 兼容与安全

- 未修改模块 ID 与 App 包名，可直接从正式 v2.5.6 升级。
- 保留原有 Root 调度器、不可变扫描快照、白名单和清理保护逻辑。
- 正式 APK 继续使用既有白泽发布证书签名。
- 模块 ZIP 内嵌 APK 与独立 APK 完全一致，并同时发布 SHA-256 校验文件与签名证书信息。

## v2.5.6

本版本完成原生 App 界面全量重构，并修复安装包扫描耗时长、手动扫描结果为空以及保留时间不可修改的问题。

### 全新原生 UI

- 首页、清理、记录、设置四个一级页面按参考视频重新设计。
- MIUIX 与 Material 3 共用相同的信息架构和交互路径，切换外观不再退回旧页面。
- 使用浅紫灰背景、大圆角分组卡片、紧凑标签页、系统设置式列表和悬浮玻璃底栏。
- 保留 Monet 动态取色、深色模式与 AMOLED 纯黑模式。
- 修复部分设备中圆角卡片内部出现矩形浅色块的渲染问题，主体卡片统一使用实色单层圆角容器。

### 安装包扫描与自动清理

- App 手动扫描始终显示当前可找到的全部 APK、APKS、XAPK 与 APKM，不再错误套用后台保留期。
- 扫描复用增量共享索引，并直接读取专用安装包索引，避免每次点击都强制重建并遍历全量文件索引。
- 后台安装包保留时间由固定 30 天改为可设置 0–365 天。
- `0 天`表示扫描到后即可进入后台自动清理范围；手动扫描始终不受该时间限制。
- 保留白名单、符号链接、文件大小、快照授权和删除前状态复核。

### 兼容与安全

- 首页一键清理与一键归类继续直接执行 Root 任务。
- 扫描工作台继续使用不可变快照，清理阶段不会重新发现或接受 UI 新增路径。
- 未修改模块 ID 与 App 包名，可直接从正式 v2.5.5 升级。
- 正式 APK 使用既有白泽发布证书签名，模块 ZIP 内嵌 APK 与独立 APK 完全一致。

## v2.5.5

v2.5.5 是针对模块卸载后 `/data/adb` 残留问题的正式热修版本。

### 修复

- 修复卸载脚本使用 `pkill -f /data/adb/modules/baize_v2` 时可能把自身终止，导致后续目录清理没有执行的问题。
- 改为枚举 `/proc` 精确终止白泽后台进程，并明确跳过卸载脚本自身和 Root 管理器父进程。
- 卸载时停止调度器、守护进程、扫描器、清理器和文件归类 Worker。
- 首轮停止后再次终止并清理，防止旧后台进程重新创建 `/data/adb/baize-v2`。
- 删除 `/data/adb/baize-v2`、旧 `/data/adb/safesweep`、新旧 `modules_update` 暂存目录以及旧 `safesweep` 模块目录。
- 同时卸载白泽 App。
- 卸载前自动恢复隔离区中的用户文件；原路径冲突时转存至 `内部存储/Download/BaiZe恢复`。
- 当前 `/data/adb/modules/baize_v2` 模块本体仍按 Magisk、KernelSU、APatch 标准流程在重启后删除。

### 验证

- 动态测试从模拟的 `/data/adb/modules/baize_v2/uninstall.sh` 自身执行卸载，确认脚本不会自杀。
- 模拟后台进程在收到停止信号后重新创建状态目录，确认第二轮清理仍能彻底删除。
- 验证隔离区文件恢复、新旧目录清理、App 卸载调用、非白泽目录保护和标准 `remove` 标记。
- 正式封包强制检查 `uninstall.sh` 可执行权限与卸载清理合同。

安装 v2.5.5 后再执行卸载，并在 Root 管理器中完成重启，模块本体目录才会由管理器最终移除。

## v2.5.4

v2.5.4 是针对 v2.5.3 自动任务状态和文件归类问题的正式热修版本。

### 修复

- 修复文件自动归类运行时，其他任务全部错误显示“后台调度正在自动恢复”的问题。
- 调度器在长时间 Root Worker 执行期间持续刷新心跳，不再把正常运行误判为服务失联。
- 首页按真实任务组显示状态：当前任务显示正在执行，其他任务显示等待当前任务完成。
- 修复自动文件归类到期后可能重建整机共享存储索引、长时间占用后台的问题。
- 自动归类改为只扫描下载、文档、蓝牙、QQ/TIM 接收目录以及浏览器和邮箱下载目录。
- 自动归类单轮最多运行 3 分钟、最多检查 2000 个文件，剩余内容留到下次继续。
- 自动归类不再逐个文件无限发送媒体扫描广播，避免完成阶段长时间卡住。
- 手动“一键归类”仍保留完整共享索引能力。

### 验证

- 通过自动归类全盘索引陷阱测试，确认自动路径不会调用整机索引。
- 通过长任务心跳、并发调度、增量索引、归类事务和深度清理回归。
- 通过 Android Release 编译、Lint、正式证书和模块内嵌 APK 一致性校验。

升级模块后请完整重启设备，使旧的后台 Worker 和调度器进程全部替换为 v2.5.4。

## v2.5.3

白泽 v2.5.3 是面向自动清理可控性、主界面可用性和规则覆盖面的正式稳定版本。该版本在 v2.5.2 不可变深度快照基础上，加入三种明确调度模式、自动驾驶策略、界面精简和一批经过保护合同筛选的安全规则。

### 定时与自动驾驶

- 新增“智能定时”“严格间隔”“每日固定”三种显式调度模式。
- 严格间隔完全遵循用户设置周期，不会被自动驾驶延长。
- 每日固定支持指定时间和补做窗口，升级旧配置时保持兼容。
- 智能定时根据清理收益调整周期；存储压力较高时会恢复基础周期。
- 手动扫描和一键清理不受自动调度模式影响。
- 电池温度继续写入诊断状态，但不再阻止任何一种定时模式执行。

### 界面调整

- 移除记录页常驻“审计中心”悬浮按钮，避免遮挡应用列表和底部导航。
- 移除设置页常驻“自动任务体检”入口；后台自愈与诊断命令仍保留。
- 删除清理计划页重复的 Root 状态行，设置页状态改为紧凑胶囊。
- 最近结果使用简短任务标题，避免长清理文本与释放容量相互挤压。
- Material 与 MIUIx 两套界面同步收紧按钮、间距、标题和底部留白。

### 清理规则

- 新增 159 条应用私有目录规则和 119 条 Android/data 规则，共 278 条。
- 覆盖微信、QQ、小红书、微博、知乎、抖音、快手、哔哩哔哩、网易云音乐、支付宝、淘宝、京东、拼多多、美团、饿了么、高德、百度地图及常见小米系统应用。
- 规则仅包含日志、崩溃记录、性能记录、网络日志、纹理和可再生 WebView 缓存。
- 新增去重、格式和用户数据路径保护合同，明确拦截下载、草稿、数据库、聊天附件和用户媒体目录。

### 可靠性

- 保留 v2.5.2 的逐文件不可变深度清理 manifest、断点续清和元数据变化保护。
- 保留统一 Root Worker、Supervisor 自动恢复、缓存并行通道和文件归类事务。
- 三模式、自动驾驶、规则安全、审计兼容、调度健康、并发任务、深度清理、增量索引和归类事务回归全部纳入正式构建验证。
- Android Release 使用固定正式证书签名，并执行 Release Lint 与 Macrobenchmark 构建。

### 正式签名

白泽正式 APK 继续使用固定 SHA-256 证书指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

## v2.5.2

白泽 v2.5.2 是面向 Root 后台可靠性与深度清理性能的正式稳定性版本。该版本冻结 v2.5.1 历史资产，修复每日定时任务字段错位风险，并把深度清理升级为逐文件不可变快照。

### 深度清理

- 深度扫描完成后固化逐文件 manifest，不再只保存目标目录。
- 每条候选记录保存设备号、inode、大小、修改时间、状态变化时间、风险级别与完整路径。
- 清理阶段直接顺序消费扫描 manifest，不再重新执行目录发现，因此避免扫描后再次全目录遍历。
- 文件删除前重复校验元数据；扫描后修改、替换或新增的内容不会删除。
- 目录记录按后序处理，只有原 inode 未变化且目录已经为空时才删除。
- 每处理一条记录都会原子保存 cursor。主动停止或 Root 任务中断后可以从准确位置继续，不从头扫描。
- 完成前保留 manifest、cursor 与累计统计；全部处理完成后才清除快照。
- 新增独立 ARM64 `baize_deep_snapshot` 引擎，并随模块一起构建、剥离、校验和封包。

### 自动任务

- 调度队列升级为固定七字段格式，空请求路径和每日周期使用显式占位符。
- 修复 POSIX Shell 折叠连续 Tab 后可能造成每日周期字段错位的问题。
- 每日任务完成日期会进行格式校验，异常时安全重建，不会在同一补做窗口内重复执行。
- 保留 Root Supervisor、快速唤醒、失败自动恢复与缓存＋文件归类兼容并发。

### 发布与升级

- v2.5.1 标签和 Release 资产已经冻结，不再被后续 `main` 提交覆盖。
- v2.5.2 使用独立、不可变标签发布；同名 Release 已存在时流水线会直接失败，禁止覆盖资产。
- 模块安装时迁移到 `deep-manifest-v1` 运行时 schema，并清理旧版目录级深度快照，避免混用格式。
- 模块 ZIP 内继续包含正式签名白泽 App、两个 ARM64 原生引擎与 SHA-256 校验文件。

### 验证

- Shell 与 BusyBox ash 语法检查。
- 两个原生引擎以警告视为错误编译。
- 每日定时、调度公平性、兼容任务并发与自动恢复回归。
- 深度 manifest 停止、续清、元数据变化保护、白名单保护、空目录后序清理与幂等回归。
- 增量索引、文件归类事务、Android Release、Lint 与 Macrobenchmark 构建。

### 正式签名

白泽正式 APK 继续使用固定 SHA-256 证书指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

## v2.5.1

白泽 v2.5.1 是基于 v2.5.0 的正式修复版本，重点重做 MIUIx / HyperOS 清理记录页。该版本使用独立标签与独立构建产物发布，不会覆盖 v2.5.0 Release。

### 主要更新

- 完整重构 MIUIx 清理记录列表，不再把最多 50 条历史记录塞进一个超高容器。
- 每条记录改为独立的 LazyColumn 项目与紧凑圆角卡片，修复长列表中的标题裁切、文字错位和异常大片留白。
- 历史任务按日期分组，任务名称、执行时间、触发方式、状态和空间统计重新排列，避免右侧大数字挤压左侧内容。
- 扫描任务明确显示“发现 / 扫描完成”，清理任务明确显示“释放 / 已清理”，不再混淆扫描体积与实际释放空间。
- 清洗历史数据中的控制字符、换行和异常空白，避免出现残缺标点、空标题及不可见字符导致的布局破坏。
- 增加浮动底栏安全留白，最后一条记录不会再被 MIUIx 导航栏遮挡。
- 保留应用与分类展开明细，同时收敛字号、状态颜色、图标和间距，使界面更接近 HyperOS 系统分组列表。
- 新增 Android UI PR 自动检查，后续界面改动会执行 Kotlin 编译、Lint 与 Macrobenchmark 装配验证。

### 安装说明

模块 ZIP 内已包含白泽 App。通过 Magisk、KernelSU 或 APatch 刷入模块后重启设备，即可使模块与 App 版本保持一致。

本次核心修复位于 App 界面；仅需要更新记录页时，也可以直接安装本 Release 中的正式签名 APK。完整升级仍建议刷入 v2.5.1 模块 ZIP。

### 正式签名

BaiZe 正式 APK 继续使用固定 SHA-256 证书指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

### 在线更新

模块的 `updateJson` 已更新为 v2.5.1。v2.5.0 Release 保留为独立历史正式版本，v2.5.1 使用新的标签、APK、模块 ZIP、证书信息和 SHA-256 文件。

## v2.5.0

白泽 v2.5.0 是基于 v2.4.0 的正式功能与界面升级版。该版本作为独立正式版本发布，不覆盖或替换原有 v2.4.0 Release。

### 主要更新

- 重构 Material 3 与 MIUIx / HyperOS 两套独立界面结构，不再只是简单换色。
- 首页改为自动任务状态、下次执行倒计时、任务计划和最近结果，六类自动任务分别显示运行状态。
- 清理页集中管理自动清理总开关、固定时间、独立周期、执行条件和清理类别。
- 记录、日志与设置页面重新整理信息层级、留白、圆角和分组样式。
- 修复深度清理执行链，定时任务统一执行 `deep-scan → deep-clean`，无候选项时正常完成，不再进入重复重试。
- 加固 Root Worker 生命周期、快速任务标记清理、调度器自动恢复和文件归类任务状态。
- 定时调度改为资源通道：应用缓存与文件自动归类可在同一批次并行执行；可能操作相同路径的删除任务仍保持互斥，避免重复删除和统计错乱。
- 任务执行期间仍可修改开关、周期与固定时间，新设置会保存并在下一次调度时生效。
- 优化自动任务默认执行条件和倒计时展示，减少长期停留在等待或唤醒状态的情况。
- 正式构建继续校验 Root 脚本、ARM64 扫描器、Android Release、Lint、Macrobenchmark、模块封包、正式签名与模块内 APK 一致性。

### 安装说明

模块 ZIP 内已包含白泽 App。通过 Magisk、KernelSU 或 APatch 刷入模块后重启设备即可。单独更新 App 时请使用本 Release 中的正式签名 APK。

从旧版本升级后必须重新刷入 v2.5.0 模块 ZIP 并重启，以清除旧的深度任务、Worker 和自动重试状态。只安装 APK 无法更新模块侧执行链。

### 正式签名

BaiZe 正式 APK 继续使用固定 SHA-256 证书指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

### 在线更新

模块的 `updateJson` 已更新为 v2.5.0。v2.4.0 Release 保留为独立历史正式版本，v2.5.0 使用新的标签、APK、模块 ZIP 和 SHA-256 文件。

## v2.4.0

白泽 v2.4.0 完成后台调度、增量索引、文件归类事务和清理页面交互的集中加固。本版本统一 App、模块、在线更新与发布产物版本为 `v2.4.0`，版本代码为 `24000`。

### 主要更新

- 清理、扫描和文件归类统一交由 Root Supervisor 调度，WorkManager 只负责低成本看门狗与恢复。
- 新增公平任务队列：手动任务优先，定时任务按超期时间排序，阻塞任务不会饿死可执行任务。
- 加固 Supervisor PID、启动时间、实例 ID、心跳和异常重启恢复。
- Root `config.conf` 成为调度配置的唯一真实来源，App 与模块状态保持一致。
- 加入立即执行、跳过本次、停止当前任务和调度中心状态展示。
- 共享存储索引支持目录指纹、增量缓存复用、inode 去重和覆盖统计。
- 文件归类支持同名跳过、自动重命名、内容去重、MediaStore 同步和多次撤销。
- 清理类别恢复折叠交互：默认收起、一次只展开一个类别，并在页面切换后保留展开状态。
- MIUIx 与 Material 两套界面同步更新，清理与调度文字统一中文化。
- 修复 QQ、浏览器、邮件等应用下载、接收、附件与导出目录的发现和排除规则。
- 正式 Release 禁止回退到 Debug 签名，缺少正式签名配置时发布流程会直接失败。

### 安装说明

模块 ZIP 内已包含白泽 App。通过 Magisk、KernelSU 或 APatch 刷入模块后重启设备即可。单独更新 App 时请使用本 Release 中的正式签名 APK。

### 正式签名

BaiZe 正式 APK 固定使用以下 SHA-256 证书指纹，后续版本必须保持一致：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

### 在线更新

模块的 `updateJson` 已指向仓库 `main/update.json`。安装旧版本后，可由模块管理器检测并下载 `v2.4.0` 正式模块。

## v2.3.0

- 新增 Root Supervisor、独立 Worker 和任务自恢复，退出 App 后任务仍可继续。
- 自动清理支持 30 分钟、1 小时和自定义分钟级周期。
- 手动与定时任务统一状态、锁、结果记录和失败恢复。
- 完善多用户、多存储卷共享索引，以及 QQ、TIM 和通用应用下载目录发现。
- 新增存储分析、大文件、重复文件、隔离区、规则校验和诊断导出后端。
- 文件归类继续使用不可变扫描计划、指纹复验、同名保护和安全撤销。
- 模块内置原生 App，不包含 WebUI，支持 Magisk、KernelSU 与 APatch。
- 补齐标准 `update.json`，已安装模块可通过兼容的面具管理器检测并下载后续更新。

## v2.2.0

本版本集中完成扫描架构、界面渲染、Root 通信与正式构建重构，不再由安装包扫描、文件归类和其他页面分别重复遍历共享存储。

### 统一扫描与覆盖报告

- 新增 Root 侧共享存储索引，一次遍历覆盖内部存储根目录、公共下载目录、应用自建目录、`Android/media/<包名>`、`Android/data/<包名>/files`、QQ/TIM 接收目录以及 Telegram、NagramX 等应用目录。
- 安装包扫描和文件归类复用同一份 NUL 安全索引，减少重复扫描和长时间占用。
- 安装包手动扫描覆盖 APK、APKS、XAPK、APKM，不受自动任务保留天数影响。
- 新增扫描覆盖报告，显示来源目录、文件数量、大小、部分不可读目录和跳过原因。

### 高刷与流畅度

- 默认启用“高刷优先”，主动请求设备支持的最高刷新率。
- 支持跟随系统、高刷优先、60Hz 省电三种策略。
- 新增自适应流畅模式：检测到连续掉帧、系统省电或严重发热时，自动暂停 Haze 模糊并缩短重动画；稳定后恢复。
- 首页显示请求刷新率和实际渲染帧率。
- Compose 页面使用稳定状态切片、服务端历史分页和轻量列表，减少无关页面重组。

### Root 通信与缓存

- 新增 AIDL 任务进度回调，常规进度通过服务端推送；低频轮询只作为异常回退。
- 历史记录新增服务端分页接口，当前页默认读取最近 30 条。
- 应用图标采用可见行异步加载、内存缓存和磁盘缓存；应用更新后通过 `lastUpdateTime` 自动换新缓存键。

### 正式构建与性能基线

- 版本：`v2.2.0`
- versionCode：`22600`
- 模块内置签名 Release APK，不再打包 Debug APK。
- 加入 Baseline Profile 和 Macrobenchmark 编译目标，用于冷启动与滚动帧耗时回归。
- GitHub Actions 会验证 QQ、Telegram、NagramX 测试目录能进入共享索引，并校验最终模块结构和 SHA-256。

安装模块后请重启设备。首次安装包扫描或文件归类会建立共享索引，后续功能会直接复用。

## v2.1.1

v2.1.1 是针对正式版真机反馈的性能与扫描覆盖热修复。

### 修复

- 清理记录页移除每条记录的软件阴影层，LazyColumn 增加稳定 key 与 contentType，降低长列表滚动时的 GPU 合成与重组开销。
- 系统即时清缓存页面恢复真实应用图标。
- 应用图标改为仅对可见行异步加载，并使用小型内存缓存，避免 PackageManager 图标解码阻塞主线程。
- 即时缓存应用列表改为轻量 Surface 行，减少大圆角卡片、阴影和分割线造成的掉帧。
- 手动安装包扫描改为扫描当前全部安装包，不再受自动任务保留天数限制。
- 安装包扫描不再只维护固定软件路径：覆盖共享存储根目录散落文件、所有自建顶层下载目录、`Android/media`、`Android/data/<包名>/files` 以及 QQ/TIM 等非标准用户文件子目录。
- 自动跳过 cache、code_cache、数据库、shared_prefs、缩略图与临时目录，避免无意义遍历。

### 版本

- 模块：`v2.1.1`
- App：`2.1.1`
- versionCode：`22501`
- 架构：`arm64-v8a`

## v2.1.0

白泽 v2.1.0 是当前 v2 系列首个正式版本，内置永久青绿色神兽 App 图标，并完成原生界面、Root 扫描与文件归类能力的整合。

### 主要更新

- 使用永久白泽青绿色神兽 App 图标，桌面图标和圆形图标统一。
- 原生 MIUIx 风格界面，移除 WebUI 资源。
- 清理流程采用扫描快照与事务式执行，减少重复扫描和误操作。
- 新增内部存储根目录、公共下载目录、QQ/TIM 接收目录归类。
- 通用发现 `Android/media/<包名>` 与 `Android/data/<包名>/files` 用户文件，覆盖 Telegram、NagramX、浏览器、网盘和下载器等应用。
- 手动入口改为一键扫描并直接归类，无需二次确认。
- 支持撤销上一次归类和真实定时归类。
- 大量文件使用 Root 端完整计划和 Binder 有界返回，避免事务缓冲区溢出。
- 同名文件直接跳过，不覆盖现有内容；缓存、数据库、缩略图、贴纸、临时文件和未完成下载默认排除。

### 版本信息

- 模块版本：`v2.1.0`
- App 版本：`2.1.0`
- versionCode：`22500`
- 架构：`arm64-v8a`

安装正式版模块后请重启设备。

## v1.0.3

### 深度扫描与缓存性能优化

- 高风险和关键风险路径默认只确认存在，不再递归统计整个目录。
- 深度目录的文件数量、容量和大文件检测合并为一次遍历。
- 通用 `cache/*`、`code_cache/*` 规则按缓存根目录合并处理，避免逐分片重复扫描。
- 深度清理保留应用缓存根目录，只清理其中内容。
- 受保护父目录仅覆盖高风险子项，低、中风险缓存子目录仍可正常处理。
- 新增深度规则解析进度、候选进度和当前路径显示。
- 单个深度目录超过 12 秒自动保护并记录，深度阶段最长 5 分钟。
- 应用缓存按 64 个目录分批扫描；慢批次自动逐目录定位并限时跳过。
- 缓存扫描阶段增加安全时限，异常目录不会长期拖住整次任务。
- 新增挂载点与跨文件系统保护，避免进入 Bind Mount、FUSE 或特殊挂载。
- 审计报告记录慢目录、挂载保护、部分扫描和已处理候选数量。
- 4,746 条深度规则全部保留，规则文件未修改。

本版本重点解决部分设备深度扫描、应用缓存扫描耗时异常的问题。

### 主要变化

- 受保护的高风险目录不再递归统计。
- 缓存通用规则合并到应用缓存根目录执行。
- 文件数量、容量和大文件状态一次遍历完成。
- WebUI 显示实时规则进度、候选进度和当前路径。
- 慢目录自动限时跳过并写入审计报告。
- 新增挂载点保护和自动化压力测试。

深度规则仍为 4,746 条，SHA-256 保持不变：

`73d4c898630a292753adca33298c8aabbf6146debf414b2cabbe6b87d1d5c31c`

## v1.0.2

### 深度安全项修复

- 修复深度规则父目录去重逻辑：高风险或关键风险父目录受保护时，不再连带跳过其内部可安全执行的低风险、中风险子规则。
- 只有已经纳入扫描候选，或清理后确认完整消失的父目录，才会覆盖后续子规则。
- 定时深度任务遇到受保护的大型高风险目录时不再反复递归统计，减少无效磁盘扫描。
- 深度扫描结果中的“可清理容量”现在只统计本次实际允许清理的候选；受保护容量单独显示。
- 全局单文件上限扩展到深度规则与卸载残留：目标中包含超限文件时整项目保持保护状态。

### 扫描快照绑定

- 手动深度清理只处理上一次深度扫描记录的具体候选，不会把扫描完成后新出现的路径顺带删除。
- 卸载残留清理同样只处理上一次扫描快照，并会在删除前再次核对应用是否重新安装。
- 深度规则 SHA、30 分钟时效、当前风险开关和白名单仍会再次校验。
- 扫描授权在清理完成后立即失效，下一次清理必须重新扫描。

### 定时与任务稳定性

- 定时等待状态相同的时候最多每 5 分钟落盘一次，避免每分钟重复写入状态文件。
- 定时分组日志超过 256 KiB 自动轮换，避免长期运行后无限增长。
- 电量、充电和温度条件共用一次电池状态读取，减少重复调用系统服务。
- 任务锁会校验进程命令行，旧 PID 被其他进程复用时不再误判为白泽任务，也不会在升级时误杀无关进程。
- 修复深度任务最后一条规则不覆盖子目录时，Shell 条件状态可能被误当成任务失败的问题。
- 收到 `TERM/INT` 信号会立即退出并释放任务锁，不再出现清理锁已释放但旧进程继续执行的情况。
- 异常遗留的 `running.env` 同名目录会自动恢复，避免状态文件写入失败。
- 每日补做窗口支持跨午夜，例如 23:30 的任务可在次日凌晨窗口内补做，并以计划周期去重避免重复执行。
- 某个定时分组失败后不再长期阻塞后续分组，全部执行结束后仍会保留汇总失败状态；已有手动任务时显示等待而不是失败。

### 配置与 WebUI

- WebUI 批量保存改为先完整校验、再一次性原子替换配置，避免保存中断造成半套配置。
- 补齐自定义规则开关与厂商日志保留天数。
- 专项扫描状态显示本次容量或项目数量。
- 后台任务监控时长会跟随“单次运行上限”，不再固定一小时。
- 页面空闲时每 30 秒轻量刷新状态，回到页面后立即同步。
- 日志、报告和历史记录增加复制按钮，日志字号略微提高。
- 累计统计文案明确“文件总计”“其中碎片”“其中隐藏”，避免把子分类误认为互斥统计。

### 规则完整性

- `config/deep.rules` 保持原样，仍为 4746 条有效规则。
- 文件 SHA-256 仍为 `73d4c898630a292753adca33298c8aabbf6146debf414b2cabbe6b87d1d5c31c`。

本版本重点修复深度规则父子路径处理、扫描快照绑定、定时任务可靠性和 WebUI 配置保存问题。

### 主要变化

- 修复高风险父目录受保护时，安全子规则被连带跳过的问题。
- 深度清理与卸载残留严格绑定上一次扫描候选。
- 卸载残留清理前重新检查应用安装状态。
- 扫描授权清理一次后立即失效。
- 白名单同时保护目标及其父子路径冲突。
- 深度规则与卸载残留受全局单文件上限保护。
- 修复旧 PID 复用、异常状态目录、终止信号和误报失败。
- 每日补做窗口支持跨午夜，单组失败不会堵塞后续任务。
- WebUI 改为原子保存，补齐扫描摘要、复制日志和配置项目。
- 4,746 条深度规则原样保留。

### 校验

`config/deep.rules` SHA-256：

```text
73d4c898630a292753adca33298c8aabbf6146debf414b2cabbe6b87d1d5c31c
```

## v1.0.1

### 界面与交互

- 重做首页、计划页和记录页的信息层级，减少独立卡片和长期显示的开关。
- 首页突出“立即清理”，安全扫描改为次级操作，停止按钮仅在需要时显示。
- 专项工具改为紧凑列表：卸载残留与完整深度清理使用“先扫描、后确认清理”的单按钮流程。
- “深度清理定时”更名为“深度安全项”，明确其自动任务只执行低风险和中风险规则。
- 应用内外缓存合并显示为空间更紧凑的“应用缓存”。
- 空文件与空目录合并显示；应用规则、隐藏垃圾和系统日志合并显示为“规则垃圾”。
- 所有被合并的原始开关均保留在“精细设置”中，功能和配置兼容性不变。
- 完成通知与零结果通知合并成三档通知模式。
- 独立定时间隔、保护阈值、保留周期、高风险放行与白名单默认折叠。
- 日志、审计报告和任务历史使用折叠区展示。

### 深度规则

- `config/deep.rules` 未删除、未修改，仍为 4746 条有效规则。
- 定时深度任务继续只执行低风险和中风险规则。
- 高风险与关键风险仍需近期扫描和手动确认，定时永不放行。

### 兼容性

- 后端配置键和任务命令保持不变。
- 从 v1.0.0 直接覆盖安装会保留配置、白名单、累计统计和历史记录。

## v1.0.0

- 完整保留 v0.9.9 的 `deep.rules`，文件 SHA-256 不变。
- 深度规则新增低、中、高、关键四级风险判定。
- 高风险与关键规则默认只扫描；定时任务永久禁止执行。
- 手动放行高风险规则时，强制要求 30 分钟内完成深度扫描，并校验规则文件 SHA-256。
- 修复缓存、规则目录、空文件和空目录统计虚高，只统计实际删除成功的项目与容量。
- 新增逐项 TSV 审计报告、受保护项目统计和最近 100 次任务历史。
- 新增卸载残留扫描与手动清理，仅识别未安装包对应的 Android/data 与 Android/obb 目录。
- 新增仅充电、系统空闲、电池温度、每日补做窗口、单次运行时长和零结果通知选项。
- 停止或超时返回代码 9，不再记为清理成功，也不会继续执行同批后续定时任务。
- WebUI 新增残留扫描、审计报告、历史记录、风险提示和动态规则统计。
- 失败任务通知与 WebUI 状态区分“失败”“停止”和“完成”。

## v0.9.9

- 新增每日固定时刻定时（小时、分钟），开启后替代间隔定时，避免重复清理。
- 定时状态卡可显示每日模式等待时刻及电量/息屏等待原因。
- WebUI 后台任务提示新增停止当前任务入口。
- 补充 QQ、微信、抖音、今日头条、百度、腾讯新闻、快手的明确崩溃记录、日志、纹理及 WebView 可再生缓存规则；不包含聊天附件、数据库、下载或媒体目录。

## v0.9.7

- 定时周期下限由 6 小时调整为 1 小时，WebUI、配置校验与服务端校验保持一致。
- 默认缓存、空文件、规则垃圾和残留碎片均设为每小时执行；深度清理仍默认关闭。
- 定时服务由每小时检查改为每分钟轻量检查：任务到期后在息屏、电量和停止条件满足时尽快执行，不再额外等待一整小时。
- 首次未初始化任务不再无条件延后一整个周期；安装脚本仍会保留升级场景的安全计时。
- 新增 `/data/adb/safesweep/scheduler.env` 记录定时状态与等待原因；各任务输出写入 `/data/adb/safesweep/logs/scheduler-<任务>.log`，便于排查。

### v0.9.8

- WebUI 增加定时服务状态卡片，直接显示已完成、等待息屏/电量、已暂停或执行失败及最后更新时间，排查不再依赖翻日志。
- 统一升级时补齐缺失配置项的默认值：空文件和隐藏垃圾均保持默认的 0 天保留，不会与新装默认设置产生差异；已保存的用户配置不受影响。
