
## v2.1.0-alpha8

- 移除普通用户可见的扫描工作进程面板，保存配置时固定使用自动策略。
- 修复受保护共享目录提前剪枝导致内部真实空目录无法进入安全快照的问题。
- 新增“文件归类”：发现公共、Android/data、Android/media 与应用私有下载子目录，按图片、视频、音频、文档、安装包、压缩包、电子书和其他整理到公共 `BaiZe归类` 目录。
- 文件归类使用不可变计划、执行前指纹复核、同名不覆盖、跨文件系统完整性校验及撤销记录。
# BaiZe v2.1.0 Alpha 7

- 新增原生“系统即时清缓存”手动页面，支持搜索、用户/系统应用筛选和最多 30 个应用多选。
- 二次确认后使用 Android PackageManager `clear --cache-only`，与白泽精准快照完全分离。
- 增加逐包超时、停止、核心包保护和系统能力检测；不支持时不执行任何回退清理。
- App / 模块版本：2.1.0-alpha7 / 22407。

# 白泽 v2.1.0 Alpha 6

## 原生性能策略面板

- 清理页新增扫描性能策略面板，MIUIx 与 Material 两套界面同步。
- 显示当前策略、实际工作进程、本机推荐、串行/双进程吞吐、提升比例、学习次数和下次复测。
- 可选择自动推荐、固定串行或固定双工作进程；保存后写入模块配置。
- 可只清除 `root-worker-profile.env` 重新学习，不删除扫描快照、历史记录或白名单。
- 自动模式继续首次串行、工作量门槛、15% 提升阈值、周期复测和失败冷却。
- 扫描与清理继续完全分离；固定双进程只影响扫描阶段。
- 保留路径索引、Android/data One-pass、原子快照和扫描后变更保护。

## 版本

- 模块：`v2.1.0-alpha6`
- App：`2.1.0-alpha6`
- versionCode：`22406`
- 扫描策略封装：`43.5-alpha6-performance-panel`
