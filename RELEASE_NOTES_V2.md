# 白泽 v2.0.4

- 将状态栏安全区从首页私有标题迁移到公共页面宿主。
- 修复 MIUIx 与 Material 的首页、清理、记录、日志、设置内容侵入时间、信号图标和挖孔区域。
- 页面背景继续延伸至系统栏，实际交互内容统一从安全区下方开始。
- 保留 v2.0.3 的每日定时、1–720 小时精确周期与 App 全面接管。

# 白泽 v2

## v2.0.3 · App 全面接管

- 移除并主动清理旧版 WebUI 目录，模块操作入口统一打开原生 App。
- App 恢复每日固定时刻设置，支持小时、分钟和 15-720 分钟补做窗口。
- 五类自动任务均支持 1-720 小时独立周期；点击周期可精确输入数百小时。
- App 重新开放执行范围、保留策略和分类保留时间，并将这些安全配置写回模块调度器。
- 修复 App 仍显示 Alpha 21、模块状态仍写入旧 Alpha 版本的问题。

白泽 v2 是面向 Magisk / KernelSU / APatch Root 环境的智能清理模块正式版，内置配套 Android App。

## 核心功能

- C 原生高速扫描：应用缓存、完整深度规则和卸载残留。
- 智能扫描：汇总应用缓存、空文件与空目录、规则垃圾、残留碎片，并在同一页面直接清理。
- 快照清理：扫描一次后保存授权快照；点击一键清理只消费刚才的结果，不会重新全盘扫描。
- 安装包扫描：识别 APK、APKS、XAPK，扫描后可按快照一键清理。
- 自动清理：支持周期、保留期和通知设置。
- 详细记录：任务历史、清理结果、原始 Root 日志和候选路径。

## 界面

- MIUIx 与 Material 两套完整界面。
- 支持 Monet 动态取色、手动强调色、明暗模式和 AMOLED 纯黑。
- 支持悬浮底栏、玻璃效果与模糊开关。
- 扫描与清理页面显示真实任务阶段、进度、当前路径和停止状态。

## 安全保护

- 白名单与应用白名单。
- 高风险和关键风险隔离，危险入口二次确认。
- 大文件、软链接、挂载点和异常路径保护。
- 卸载残留清理前复核应用是否重新安装。
- 扫描后新建或修改的文件不会被旧快照删除。
- 快照过期或校验失败时直接提示重新扫描，不会偷偷回退到全盘扫描。

## 安装

1. 在 Root 管理器中刷入 `BaiZe-v2.0.3-Module.zip`。
2. 重启设备。
3. 模块会安装或更新配套白泽 App。
4. 首次启动授予 Root 权限后即可使用。

## 版本

- 模块：`v2.0.3`
- App：`2.0.3`
- versionCode：`22330`
- 最低 Android：Android 8.0（API 26）
- 原生扫描架构：ARM64
