# 白泽 v2.1.0 Alpha 4

## 有限并发根扫描

- 在性能足够且内部、外部根目录同时存在时，最多启用 2 个受控工作进程。
- 内部 `/data/user*` 与外部 `Android/data` 根目录互不重叠；每个目录树内部仍保持顺序遍历。
- 低内存、低核心设备或缺少任一根目录时自动回退串行扫描。
- 可通过 `scan_root_workers=0/1/2` 选择自动、串行或双工作进程。
- 两个工作进程只写各自临时结果，全部成功后才合并并原子发布新快照。
- 任一工作进程失败或停止时保留上一份有效快照，不发布半成品。
- 新增墙钟耗时、内部/外部工作进程耗时和并发重叠系数。
- 保留路径白名单索引、受保护子树剪枝、Android/data 单次枚举和逐文件不可变快照。

## 版本

- 模块：`v2.1.0-alpha5`
- App：`2.1.0-alpha4`
- versionCode：`22404`
- 原生引擎：`43.3-alpha4-bounded-parallel`
