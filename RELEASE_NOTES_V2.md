# 白泽 v2.1.0 Alpha 2

## 外部存储 One-pass

- 手动扫描应用缓存或卸载残留时，只枚举一次每个用户的 `Android/data` 顶级应用目录。
- 已安装应用在同一次枚举中生成外部缓存不可变快照。
- 未安装应用在同一次枚举中生成卸载残留独立快照。
- `Android/obb` 与 `Android/media` 继续按卸载残留高风险候选处理，不混入应用缓存。
- 两类结果保存为不同快照，清理入口和授权范围互不混用。
- 扫描后新增或元数据变化的缓存文件仍由 v2.0.5 的不可变快照校验跳过。

## 性能指标

- `one_pass_app_dirs`：本次读取的 `Android/data` 应用目录数。
- `one_pass_installed_dirs`：已安装应用目录数。
- `one_pass_orphan_dirs`：卸载残留目录数。
- 继续记录安装包索引条数、查询次数、首项耗时、总耗时和吞吐。

## 版本

- 模块：`v2.1.0-alpha2`
- App：`2.1.0-alpha2`
- versionCode：`22402`
- 原生引擎：`43.1-alpha2-one-pass`

该版本仍为性能预览，不合并到稳定分支。
