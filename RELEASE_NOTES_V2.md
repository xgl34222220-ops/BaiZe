# 白泽 v2.1.0 Alpha 1

这是 v2.1.0 性能版本的第一阶段预览。重点不是增加删除规则，而是减少重复 I/O，并建立可验证的扫描基准。

## 本阶段完成

- 卸载残留扫描启动时一次性读取所有 Android 用户的安装包列表。
- 安装包查询改为排序内存索引和二分查找，不再为每个 `Android/data`、`obb`、`media` 目录反复打开包列表文件。
- 应用缓存包白名单改为排序索引查询。
- 原生扫描增加访问文件数、访问目录数、首个结果时间、引擎耗时和项目吞吐统计。
- 进度文件限制为约 250 毫秒最多更新一次，减少状态文件写入对扫描的影响。
- v2.0.5 的逐文件不可变快照、自动缓存事务和失败熔断全部保留。

## 尚未完成

- 外部 `Android/data` 缓存与卸载残留的真正单次遍历合并。
- 规则和路径白名单前缀树。
- 顶级根目录有限并行。
- 独立系统即时清缓存模式。

## 版本

- 模块：`v2.1.0-alpha1`
- App：`2.1.0-alpha1`
- versionCode：`22401`
