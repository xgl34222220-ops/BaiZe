# 白泽 v2.4.1

白泽 v2.4.1 是面向智能扫描与一键清理链路的可靠性更新。本版本统一 App、模块、在线更新与发布产物版本为 `v2.4.1`，版本代码为 `24001`。

本次重点不是继续堆叠清理规则，而是确保扫描结果可以被准确、稳定并可恢复地执行：点击清理不会重新全盘扫描，停止或异常中断后也不会丢失剩余任务。

## 主要更新

- 智能扫描完成后生成独立 `planId`，固定缓存快照、安全项目快照、候选数量、生成时间和设置摘要。
- App 页面重建、进程被系统回收或 Root 服务重启后，可恢复并验证同一份清理计划。
- 点击“一键清理”只消费已保存的不可变快照，禁止回退到扫描接口。
- 快照超过 30 分钟、白名单变化或清理设置变化时，仅提示重新扫描，不会偷偷启动扫描。
- 清理前建立 Root 事务检查点；停止、超时、Binder 异常或进程中断后保存剩余候选。
- 已完成或确认失效的候选不会重复处理；部分删除、失败和未执行候选保留为“继续清理”。
- 新增候选级最终状态：实际清理、扫描后变化、受保护、部分清理和失败。
- 扫描候选、授权候选、处理候选、删除文件、错误数量和实际释放空间分别统计，不再混用。
- 按应用缓存、空项目、规则垃圾、残留碎片及风险等级生成真实分类结果。
- 无法从逐项报告确定归属的释放空间单独标记，不再伪造分类数据。
- 修复模块 `latest.env` 将删除文件数错误当作低风险候选数的问题。
- README、安装提示、App、模块、更新通道、封包规则和 CI 版本统一更新。

## 安全行为

- 扫描后新建或发生变化的文件不会被旧计划删除。
- 高风险与关键风险项目仍默认只扫描，不参与自动清理。
- 软链接、挂载点、系统关键路径、白名单和单文件上限保护保持启用。
- 清理失败不会触发全盘扫描；用户可以继续剩余计划或主动放弃计划重新扫描。

## 安装说明

模块 ZIP 内已包含白泽 App。通过 Magisk、KernelSU 或 APatch 刷入模块后重启设备即可。安装脚本会尝试更新内置 App；失败时可手动安装 ZIP 内的 `app/baize.apk`。

从 v2.4.0 更新时会保留配置、白名单、历史记录和调度设置。正在执行的旧扫描快照会在安装时清除，更新完成后请重新执行一次智能扫描。

## 正式签名

BaiZe 正式 APK 固定使用以下 SHA-256 证书指纹，后续版本必须保持一致：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

## 验证范围

- 清理计划持久化与禁止二次扫描契约
- 中断事务恢复与完成态统计契约
- 真实分类结果模型契约
- Root Shell 语法和现有调度、索引、归类回归测试
- Android Release、Debug Kotlin、Lint 与 Macrobenchmark
- ARM64 原生扫描器
- 模块封包、内置 APK 一致性和签名证书校验

## 在线更新

模块的 `updateJson` 指向仓库 `main/update.json`。v2.4.1 正式发布并合入 `main` 后，模块管理器即可检测并下载对应正式模块。
