# 白泽 v2.4.1 Beta 1

这是基于当前最新第十阶段代码构建的预发行测试版，标签为 `v2.4.1-beta.1`。App 与模块内部版本仍为 `v2.4.1 / 24001`，不会改动 `main` 分支的稳定更新通道。

本次预发行版重点解决“扫描后清理又重新扫描”、中断后无法继续、结果统计不真实、候选不可选择、后台任务长期熔断，以及规则覆盖不足等问题。

## 核心清理链路

- 智能扫描完成后生成不可变清理计划，持久化缓存快照、安全项目快照、候选数量、设置摘要和计划编号。
- 点击“一键清理”只消费已经保存的快照，禁止重新调用扫描接口。
- 页面重建、App 被系统回收、Root 服务重启、停止、超时或 Binder 异常后，可以直接继续剩余计划。
- 已完成项目不会重复执行；部分删除、失败和未执行项目保留为“继续清理”。
- 清理前可按应用、类别和风险查看候选，并支持逐项、整组、全选或取消选择。
- 最终计划在 Root 侧原子固化，未选择项目不会进入清理事务。

## 结果与报告

- 分开统计扫描、授权、处理、实际清理、发生变化、受保护、部分完成和失败候选。
- 展示实际释放空间、文件与目录数量、类别、风险、规则来源、命中原因和处理原因。
- 支持清理前后对比、逐项分页报告、搜索和最近报告恢复。
- 可从报告直接保护整个应用或指定可变数据路径。
- 无法准确归属的释放空间单独记录，不伪造分类结果。

## 规则与安全

- 应用私有规则扩展至 430 条，Android/data 外部规则扩展至 232 条。
- 连同隐藏规则 22 条和深度规则 4,746 条，内置有效规则总数达到 5,430 条。
- 新增常见社交、短视频、影音、音乐、电商、外卖、地图、浏览器及小米系统应用的日志、崩溃、性能、纹理、GPU、WebView 和网络诊断缓存规则。
- 不加入下载、草稿、聊天附件、数据库或用户媒体等高风险内容。
- 高风险与关键风险项目仍默认只扫描；软链接、挂载点、系统关键路径、白名单和单文件上限保护继续生效。

## 后台任务恢复

- 定时深度清理统一改为 `deep-scan → deep-clean`，保证清理消费同一份快照。
- 真实失败采用 5、15、30、60、120 分钟渐进退避，最长 2 小时，不再固定熔断 6 小时。
- 用户主动立即执行或唤醒任务时，会清除旧退避并立即重试。
- 文件归类部分项目因权限、状态变化或目标冲突被跳过时，不再把整批任务判定为硬失败。
- 模块升级时自动清理旧版熔断计数和暂停文件。

## 产品调整

- 移除 App 用户侧的在线规则中心、官方更新入口和网络权限。
- 保留仓库内签名规则构建、验签和发布工具，供维护者离线审计。
- 模块 ZIP 内已包含正式签名白泽 App，可通过 Magisk、KernelSU 或 APatch 刷入后重启。

## 正式签名

APK 使用白泽固定正式证书，SHA-256 指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

## 预发行说明

- 本版本用于真机验证最新完整清理链路和后台调度行为。
- 预发行标签为 `v2.4.1-beta.1`，不会替换稳定版 `v2.4.1` 标签。
- `main/update.json` 不会指向此 Beta，因此稳定用户不会自动收到该测试版。
