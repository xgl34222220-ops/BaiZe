# 白泽 v2.5.1

白泽 v2.5.1 是基于 v2.5.0 的正式修复版本，重点重做 MIUIx / HyperOS 清理记录页。该版本使用独立标签与独立构建产物发布，不会覆盖 v2.5.0 Release。

## 主要更新

- 完整重构 MIUIx 清理记录列表，不再把最多 50 条历史记录塞进一个超高容器。
- 每条记录改为独立的 LazyColumn 项目与紧凑圆角卡片，修复长列表中的标题裁切、文字错位和异常大片留白。
- 历史任务按日期分组，任务名称、执行时间、触发方式、状态和空间统计重新排列，避免右侧大数字挤压左侧内容。
- 扫描任务明确显示“发现 / 扫描完成”，清理任务明确显示“释放 / 已清理”，不再混淆扫描体积与实际释放空间。
- 清洗历史数据中的控制字符、换行和异常空白，避免出现残缺标点、空标题及不可见字符导致的布局破坏。
- 增加浮动底栏安全留白，最后一条记录不会再被 MIUIx 导航栏遮挡。
- 保留应用与分类展开明细，同时收敛字号、状态颜色、图标和间距，使界面更接近 HyperOS 系统分组列表。
- 新增 Android UI PR 自动检查，后续界面改动会执行 Kotlin 编译、Lint 与 Macrobenchmark 装配验证。

## 安装说明

模块 ZIP 内已包含白泽 App。通过 Magisk、KernelSU 或 APatch 刷入模块后重启设备，即可使模块与 App 版本保持一致。

本次核心修复位于 App 界面；仅需要更新记录页时，也可以直接安装本 Release 中的正式签名 APK。完整升级仍建议刷入 v2.5.1 模块 ZIP。

## 正式签名

BaiZe 正式 APK 继续使用固定 SHA-256 证书指纹：

`9E:FA:84:80:01:CC:DC:16:8E:A9:67:53:D3:32:B1:71:3E:26:68:BA:6A:2F:A2:2D:5B:FD:98:DE:65:DB:1F:0F`

## 在线更新

模块的 `updateJson` 已更新为 v2.5.1。v2.5.0 Release 保留为独立历史正式版本，v2.5.1 使用新的标签、APK、模块 ZIP、证书信息和 SHA-256 文件。
