# 白泽 v2.6.0

本版本集中完善清理链路的可恢复性、删除安全边界、统计可信度、存储分析能力与正式发布工程，面向 Android 8.0+ ARM64 设备。

## 不可变快照与恢复

- 合并重复扫描入口，由统一扫描工作台承载安全扫描、快照确认与清理。
- 清理严格消费扫描时生成的不可变快照，不在确认后重新枚举目录。
- 持久化清理游标与任务状态，覆盖取消续作、进程重启和设备重启后的恢复。
- 清理前复核设备、inode、大小、时间戳、规则与白名单摘要，过期或不一致时拒绝删除。

## 安全保护与多用户

- 类型化排除规则支持按工具作用域、Android 用户、路径和应用进行保护。
- 新增前台活跃应用防护，降低清理正在使用应用缓存的风险。
- 深度定时任务在扫描与删除两端都锁定为低/中风险；高风险和关键风险仍只能手动确认。
- 卸载残留、白名单、符号链接、跨挂载点、个人文件与关键系统路径保护继续默认启用。

## 可信统计与隔离恢复

- 模块清理结果统一写入幂等事件账本，避免重试或并发任务重复累计。
- 历史、诊断和首页统计以实际删除复核结果为准，不再把计划大小当作释放空间。
- 完善隔离区记录和恢复状态，失败结果可追踪，恢复操作可审计。

## 存储工具与调度

- 新增只读存储分类、大文件与精确重复文件分析，不自动删除个人文件。
- 改进 Android 多用户目录、`Android/data`、`Android/media` 与常见接收/附件目录索引。
- 调度器完善资源通道、公平队列、到期状态和健康诊断，保留 Doze 与厂商省电条件说明。

## 工程与发布

- 将历史临时构建、诊断和逐版本发布工作流归档，统一为一条 CI 与一条正式发布工作流。
- CI 统一执行 Root 脚本语法、规则编译、安全契约、ARM64 原生引擎、App、Lint 与基准目标验证。
- 正式发布要求既有白泽证书签名、当前提交 CI 通过、两个不同 ARM64 真机验收，且其中一个必须为 Android 8 / API 26。
- tag 与 Release 资产保持不可变；失败发布必须使用新版本，禁止覆盖旧 tag。
