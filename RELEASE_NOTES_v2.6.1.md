# 白泽 v2.6.1

本版本是 v2.6.0 的安装热修版，主要修复 KernelSU/MMRL 等环境下刷入失败的问题。

## 修复

- 修复部分 Root 管理器解压模块 ZIP 后不保留原生 ELF 可执行位，导致安装器误报“没有适配当前架构的原生扫描器”。
- 安装器现在会先为打包的 `baize_engine` 与 `baize_deep_snapshot` 恢复 `0755` 权限，再进行 ABI 匹配。
- 新增安装权限回归测试，模拟 `arm64-v8a` 引擎以 `0644` 解压后仍可正确恢复并匹配。
- 保留 v2.6.0 的清理、安全、规则库、多 ABI、断点续清与在线更新改进。

## 在线更新

已安装旧版本时，在 Magisk、KernelSU 或 APatch 的模块页面刷新后更新到 v2.6.1 即可。在线更新继续使用 GitHub Raw 稳定镜像。
