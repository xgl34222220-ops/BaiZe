# 白泽 v2.6.3

v2.6.3 重点提升大规模共享存储扫描、白名单保护和文件归类的性能与可靠性。

## 性能
- 白名单一次预加载并在 shell 内匹配，移除扫描过程中的高频外部进程调用。
- 原生目录遍历改用 `fstatat` / `d_type`，减少深层路径重复解析。
- 停止/超时检查改为 512 条目节流，并在目录边界立即检查。
- 共享存储索引改由原生引擎完成，避免逐文件 shell fork。

## 安全与稳定性
- 修复白名单初始化顺序，避免保护列表未加载时继续清理。
- 原生索引 seen 集合改为三态返回，OOM 不再被误判为新文件。
- 索引输出、seen 持久化与 summary 写入失败会返回非零状态，不再缓存部分结果。
- 文件归类媒体库刷新改为 durable pending/inflight 队列，由 RootService 恢复和消费，不再逐文件调用 `am broadcast`。
- 文件归类统一分类、多用户路径和撤销安全逻辑。

## 发布
- 版本升级到 v2.6.3。
- 模块打包脚本的版本校验改为读取 `module.prop`，避免残留旧版本硬编码。
- 深度规则打包校验改为对比当前源码 SHA256，避免规则更新后被旧硬编码哈希误拦截。
