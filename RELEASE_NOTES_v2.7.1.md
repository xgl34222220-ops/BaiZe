# 白泽 v2.7.1

本版重点修复 App 反复断开、连接，精简重复恢复和轮询逻辑。

- 修复归类、APK 扫描和清理请求异常被误判为服务断连的问题。
- 合并重复连接请求，连续失败采用有限重试，避免无限断开、重连；重试耗尽后明确提示手动重连。
- 手动重连保留后台任务状态，首次启动继续恢复两个引擎中的任务。
- 修复任务状态读取失败被误判为任务结束的问题。
- 删除首页重复唤醒调度器、重复进度轮询及 WorkManager 内部嵌套重试，降低无效开销。
- 修复进度回调并发广播隐患，取消后台任务时正确释放服务绑定。
- 保留白名单、关键路径保护与不可变清理快照。

升级请安装新版模块，并将白泽 App 更新到 2.7.1；模块内已包含同版正式签名 APK。连接修复仍需结合实际设备反馈验证。
