# 白泽 v2.8.0

本版完善连接恢复与可选择清理，保留应用明细并减少重复扫描开销。

- 修复 Root 启动失败或无回调后一直显示“连接中”的路径，提供超时结果、手动重连与本地连接诊断。
- 首页手动扫描和深度清理进入统一选择页，按应用显示图标、垃圾分类、完整路径和处理原因。
- 增加未选择、不可清理等筛选。高风险默认不勾选，可明确逐项选择；白名单和关键数据保留原因说明。
- 扫描结果与选择会保存，重新进入不再自动重扫。清理后保留已处理、部分处理及未勾选项目。过期结果仍可查看。
- 修复最近清理应用明细重进 App 后丢失；保存应用标识，重新加载图标。
- 复用同轮规则的目录列表，整份快照共用大小统计预算，减少逐页等待；取消前 2,000 条结果及规则展开前 256 个匹配的静默截断。
- 增加 Crashlytics、Bugly、应用运行与推送诊断日志的手动扫描规则，详情见 docs/CLEANING-RULES.md。
- 媒体队列启动处理移至后台，修复并发临时文件冲突，安装器准确报告失败原因并阻止自动降级。

请同时更新模块和 App；模块已内置同版正式签名 APK。首次拒绝 Root 后若刚修改授权，请重启 App 再连接。实际扫描耗时取决于设备及文件量；尚未完成所有 ROM 的真机验证。

本版未接入捐赠。更完整的审查和后续改进清单见 docs/AUDIT-v2.7.1-followup.md。
