# 白泽 v2.8.1

修正 v2.8.0 连接恢复相关问题。

- 修复 Root 媒体扫描队列对 Application Context 的错误假设：独立 Root 进程可以没有 Application，保留有效的服务 Context，避免队列启动或提交时空值异常导致整个 Root 进程退出。
- 修复恢复次数耗尽后首页与设置仍显示“恢复中”；各页面统一显示实际连接状态。
- 增加独立 Root 崩溃记录，在“设置 → 崩溃与诊断信息”中查看和复制，断开时仍可读取；App 新增崩溃记录也包含实际版本号。

用户日志显示主服务和缓存服务通过模块校验后约一秒同时断开，与上述启动路径相符；日志没有 Root 异常堆栈，仍需真机验证是否完全解决。

请更新模块与内置同版 App。保留 v2.8.0 的可选择清理、扫描结果保存及规则优化。
