# 白泽 v2.8.3

本版同时更新 App 和模块，包含 Root 连接恢复修复及扫描、清理性能优化。建议成对更新，当前任务结束后重启设备，避免旧 Root 服务继续运行。

## Root 连接与诊断

- 修复启动广播已收到但握手失败后绑定请求残留，以及断连回调内重新绑定被旧连接清理覆盖的问题。
- 区分 Root 不可用、启动失败、启动超时、Binder 握手失败和空绑定；自动恢复仍有重试上限，手动停止不会被延迟回调撤销。
- 对比 App、已安装模块和正在运行的 Root 服务版本。断开连接后的版本记录明确标为历史观测，历史崩溃日志不再被自动解释为当前故障。

## 扫描与清理

- 首页一键清理保留原有完整分类、后台 Worker 和安全规则，优化兼容引擎处理候选清单的重复工作。不是把完整清理替换成仅清缓存。
- 安全扫描复用同一次遍历中的路径、文件元数据和目录列表，减少重复规范化和空目录检查。受保护目录的空壳检查仍保留，删除前重新校验，不使用扫描缓存授权删除。
- 扫描结果按批展示预览，明确区分扫描、读取结果和整理显示阶段。完整读取前禁止选择和清理，完成后再统一排序；分页错误、停止和过期快照不会变成可执行的完整结果。
- 模块深扫将目标发现与不可变文件清单生成衔接，减少重复递归统计；保留风险等级、白名单、大文件、挂载点和符号链接检查。
- 模块深度清理使用批量持久断点，减少逐条同步写入；同次设备启动内可恢复 Root 进程中断，恢复时仍重新检查待处理文件身份。异常窗口内无法证实的删除结果单独记录为不确定，不冒充已确认删除量。设备重启后，已开始清理的旧快照必须重新扫描，防止把断电后失效的操作记录算成新结果。

## 发布与限制

- APK 使用既有正式签名，模块内 APK 与单独下载 APK 一致；正式产物和下载镜像验证通过后才推进 OTA。
- 未缩减清理规则库或降低风险上限。
- 主机回归测试和合成目录测试不能替代 Android 真机测试。本版不承诺固定提速倍数；实际耗时取决于文件数量、目录结构、存储性能和 Root 环境。
- Root 启动超时仍不能中断主线程上已经阻塞的同步 Binder 调用；文件系统时限仍为协作式检查。
