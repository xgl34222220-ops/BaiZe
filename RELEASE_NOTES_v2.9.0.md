# 白泽 v2.9.0 正式版

## 清理效果

- 安装包改为独立定时任务：单独设置每小时/每天等周期，不再跟随规则垃圾任务。
- 统一扫描与删除范围，补齐多层下载目录、应用缓存里的安装包、多用户/工作资料、外置存储；修复“扫到但删不掉”。
- 手动安装包扫描逐包显示文件名、路径和体积；保留期只影响后台任务。
- 手动缓存扫描显示当前缓存，不再受后台保留天数限制。
- 后台安装包结果区分已删除和期限内保留数量。

## 速度与稳定性

- 安装包走专用扫描，不再先建立全部文件与重复文件索引。
- 共享索引减少逐目录进程和重复遍历，复用已有原生分桶能力。
- 修复安装包扫描期间被其他任务误判为失效任务锁的问题。
- 索引缺失、读取失败和终止信号处理更明确。

## 文件归类与界面

- 补齐 UC、夸克、网盘、Telegram 等公共来源；保留预览、同名冲突处理和撤销。
- 首页、清理、记录和设置统一提高辅助文字可读性、触控尺寸和卡片层次。
- 安装包执行周期、保留期和快捷选择集中显示；首页新增安装包定时状态。

## 升级说明

请刷入本版完整模块，确保 App 与模块都为 2.9.0。现有安装包保留期会保留，独立周期继承原规则任务。

**如果要每小时清理已下载的安装包：清理 → 计划 → 安装包计划，开启自动清理，执行间隔设为 1 小时，保留时间选择“不保留”。** 保留 30 天时，每小时执行也只会删除 30 天以前的包。任务仍遵守自动任务总开关、息屏/充电等条件；严格间隔模式不按清理收益延长周期。

本机测试已覆盖 121 个安装包和保护边界。6000 文件共享索引一次比较从 6.022 秒降为 0.184 秒，仅供说明优化方向，实际速度随手机和存储情况变化。
