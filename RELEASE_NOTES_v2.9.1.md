# 白泽 2.9.1

这次重新梳理了整个界面和清理执行链路，修复 2.9.0 未覆盖的清理失效问题。

## 界面

- 对照洛书当前界面重做首页、清理、自动计划、记录、日志、设置和外观，统一标题、留白、颜色层次、导航和主操作。
- 缓存、安装包、规则、深度清理、文件归类、白名单及审核页面统一设计；扫描容量、执行状态、明细和下一步操作集中展示。
- 首页区分未连接、扫描中、异常、无候选与有结果，避免扫描不完整时显示“已清爽”。
- 自动计划集中显示各类开关和间隔；安装包保留期限独立说明，手动扫描不受后台期限限制。

## 清理与规则

- 修复外部应用缓存扫描成功后，在删除阶段被错误路径判断拒绝的问题。
- 一个超大缓存文件不再屏蔽整个应用的普通缓存；变化文件和白名单继续保留。
- 规则目录内文件合并扫描、精确按保留时间筛选，减少重复查找与进程启动。
- 修复通配规则风险、重叠规则和保护目录的处理；移除与保护规则冲突的下载暂存目录清理，保留 `.nomedia`。
- 安装包快照增加文件身份校验，保留扫描后被替换或更新的文件。

## 扫描与归类

- 重复文件先按大小和文件前缀筛选，再计算完整哈希，减少无效读取。
- 大文件和存储分析复用统一元数据，不再重复逐文件统计；补齐索引配置失效与清理后的刷新。
- 自动归类按剩余队列继续处理，避免长期卡在不支持或冲突文件；失败保留可恢复结果，增强停止与撤销一致性。

## 自动执行

- 智能计划到期建议与真实运行时间分开保存，修复切换严格间隔后仍被旧延迟影响的问题。
- 修复取消后立即重复启动、任务启动争用、守护进程重启后的重复执行及旧结果误报。
- 统一活跃任务识别，降低界面刷新或修复动作误清任务锁的可能。
- 每轮配置和运行条件复用，减少重复系统查询。

## 升级

请刷入完整模块并重启。已有开关、白名单与保留期限继续保留。需要每小时清理新下载的安装包时，在“清理 → 自动计划”启用安装包，将间隔设为 1 小时、保留期限设为不保留，并检查执行条件。

版本 2.9.0 已发布的资产保持原样，2.9.1 使用同一正式签名升级。
