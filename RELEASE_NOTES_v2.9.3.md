# 白泽 2.9.3

修复大量扫描结果无法提交清理、已有规则未被按项扫描读取的问题，并继续统一主页面与清理工具的布局。

## 清理与规则

- 修复扫描、所选路径和清理明细过大时出现 `data parcel size` 的错误。四类 Root 服务使用受限的文件描述符传输 JSON，避免把整份结果塞进 Binder 消息。
- 清理失败保留列表与选择；结果无法确认时要求重新扫描，不自动重复执行删除。失败提示使用错误状态，任务历史不会把不完整清理记为成功。
- 修复按项扫描没有解析 `app.rules`、`external.rules` 和 `hidden.rules` 实际格式的问题，让已有 299 条应用规则、159 条外部规则和 21 条隐藏垃圾规则参与扫描。
- 增补 WebView 缓存、设备加密存储中的应用缓存、多用户路径与应用诊断日志；专项复核规则由 10 条增加至 35 条，完整深度规则仍为 4,714 条。
- 有保留期的目录按文件年龄筛选，删除前再次检查保留期限；嵌套回收目录和宽目录规则也保留近期文件及占位标记。
- 统一 Android 存储别名的去重与白名单比较，避免同一文件重复显示或绕过保护；修复固定系统日志目录只能扫描、无法清理内容的问题，保留目录本身。
- 修复断点续清在原生清理异常后自动再次执行删除的问题，只有明确的快照过期才进入恢复路径。
- 专项清理增加“按项选择清理”。中风险可手动选择；高风险需逐项勾选并确认本次清单。关键数据、白名单、路径和快照校验继续生效，定时任务不会因此获得高风险删除权限。

## 界面与交互

- 统一主页面与工具页面的字阶、间距、按钮、图标、圆角和浅深色层次，减少重复的大卡片与说明。
- 扫描结果改为应用分组和连续文件行，默认折叠分组；详情可查看完整路径。筛选改为菜单，清理操作固定在底部，列表为实际按钮高度留出空间。
- 重排规则与保护、清理策略、即时缓存、断点续清、受保护项目和隔离区；选择、查看明细与执行操作各自明确。
- 首页、记录、设置、安装包、文件归类、外观、日志与白名单同步采用统一排版；白名单筛选和长选项适配窄屏换行。
- 页面状态区分完成、失败、中止与部分完成，减少错误后出现空结果引导或成功图标的情况。

请刷入完整模块并重启，更新 App 与 Root 服务；原有配置继续保留。扫描范围扩大后的实际耗时取决于文件数量和设备，本版不承诺固定提速比例或清除所有占用。

[实际页面渲染与验证范围](docs/ui/v2.9.3/README.md) · [规则来源、覆盖范围与保护边界](docs/CLEANING-RULES.md)
