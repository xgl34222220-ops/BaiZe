# 白泽 3.0.0 正式版 · 洛书同源 UI

2026-09-13 正式重构包，内部构建号 **30001**，替换原 30000 构建。版本名称仍为 3.0.0，不是预览版；后续构建号须大于 30001。

## 界面与底栏

- 首页、设置按洛书当前实际使用的页面重构：统一渐变主卡、分组列表、字号、图标、圆角与深浅色表面；自动任务和服务详情独立成页。
- MIUIX 底栏使用洛书同源的背景采样、外壳折射、移动透镜、高光和弹簧动画。保留白泽的首页、清理、记录、设置四个入口。
- 进入自动任务与连接诊断详情时隐藏底栏，返回恢复设置项选中。
- 关闭效果、无硬件加速或旧系统回退时使用不透明底色与非着色器裁剪，避免后方文字穿透及软件绘制异常；支持的设备仍使用硬件玻璃效果。
- 保留 Material 独立界面、动态取色、主题色与 AMOLED 选项。

## 保留的 3.0.0 修复

- 请求与响应文件由 App 创建，Root 服务写入传入的结果文件，只返回完成状态，修复此前报告的“服务结果未确认 / Transaction failed on small parcel”传输问题。
- 四类 Root 服务使用文件通道传输大批量路径和结果，保留大小上限与通道校验，避免整份 JSON 塞入 Binder 缓冲区。
- 服务断开或响应未确认时不自动重放清理，保留扫描列表与勾选；失败原因可在结果页和任务详情中查看。
- 本轮不修改清理规则、清理引擎与高风险确认边界。

## 安装与升级

在 Magisk、KernelSU 或 APatch 中刷入 **BaiZe-v3.0.0-Module.zip**，随后重启设备。模块内置与独立 APK 字节一致的正式签名白泽 App，升级保留原配置。已安装旧 3.0.0 的设备也应更新到内部构建号 **30001**。

独立 APK 同时提供；完整更新建议使用模块 ZIP，避免 App 与模块来源不一致。不需要卸载旧正式版或清除应用数据。

## 验证边界

发布流程执行 Debug/Release 单元测试、界面交互回归、lint、核心与调度合约，并校验正式签名、内置 APK 一致性和 SHA-256。旧版下载文件先备份，下载镜像与正式附件验证后才推进在线更新。

自动回归与软件渲染不能代替 **K80 至尊版、一加 15** 的真机验证；本轮尚未收到这两台设备的 GPU 折射、触控、滚动及重启后服务实测结果，不声称已完成真机兼容验收。

[规则来源与保护边界](docs/CLEANING-RULES.md) · [同源 UI 参考](docs/UI-LUOSHU-BASELINE.md) · [底栏实现](docs/UI-LUOSHU-DOCK.md)
