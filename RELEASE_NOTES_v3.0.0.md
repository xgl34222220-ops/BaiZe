# 白泽 3.0.0 正式版

本版集中修复扫描时出现“服务结果未确认 / Transaction failed on small parcel”的 Root 文件传输兼容问题，并包含 2.9.3 的规则、清理与界面改进。

## 修复内容

- 请求与响应文件统一由 App 创建，Root 服务写入传入的结果文件，只返回完成状态；避免将 Root 私有目录创建的结果文件描述符返回 App 时遭遇跨权限限制。
- 四类 Root 服务同步接入新传输方式，覆盖主服务、缓存、按项扫描与应用详情相关操作。
- 大批量路径和结果继续通过有大小上限的文件通道传输，避免整份 JSON 占用 Binder 消息缓冲区；请求和结果通道在任务执行前校验。
- 服务断开、响应读取失败或结果未确认时，不自动重放清理；保留列表与勾选，按任务记录和重新扫描结果确认后再操作。
- 扫描失败时在结果页直接显示具体原因，完整信息仍可在任务详情中查看。

## 安装与升级

在 Magisk、KernelSU 或 APatch 中刷入完整模块 ZIP，随后重启设备。模块内置同一正式签名的白泽 App，升级保留原有配置；重启后 App 与 Root 服务使用 3.0.0。独立 APK 同时提供，完整升级请使用模块 ZIP。

## 验证范围

新增传输测试及 Android 自动回归已通过，覆盖大数据传输、无效请求与响应通道、独立读写位置、结果未确认不重放及失败原因展示。

此前报告故障的两台手机尚未反馈更新后的实测结果，因此不将自动测试等同于真机兼容验证。升级后仍需确认扫描、展开结果、勾选清理及重启后的服务连接。

历史改进：[2.9.3 发布说明](RELEASE_NOTES_v2.9.3.md) · [规则来源与保护边界](docs/CLEANING-RULES.md)
