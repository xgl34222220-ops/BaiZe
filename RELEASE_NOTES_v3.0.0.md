# 白泽 3.0.0 正式版 · 启动闪退修复

内部构建号 **30002**，修复 30001 正式包打开即闪退的问题；版本名称仍为 3.0.0。

## 本次修复

- 已从公开发布的 30001 签名 APK 在 Android 16 模拟器复现：`InitializationProvider` 初始化 WorkManager 时，`WorkDatabase_Impl` 的无参构造器在 Release 压缩过程中被移除，报 `NoSuchMethodException`。崩溃发生在首页和 App 自身崩溃记录器启动之前。
- 为 WorkManager 的 Room 数据库反射入口补齐类名、成员和构造器保留规则；继续保留 Release 压缩，不停用后台任务，不删除数据库，不重置用户设置。
- 洛书同源首页、设置、液态玻璃底栏与动画保留，不用关闭玻璃效果掩盖启动问题。
- 正式发布增加安装最终签名、压缩 APK 的 Android 启动检查，不再仅依赖 JVM 测试或软件渲染截图。

## 安装

已装 30000 或 30001 的用户，直接覆盖安装本次 APK；完整模块更新刷入 **BaiZe-v3.0.0-Module.zip** 后重启。沿用原正式签名，模块内置 APK 与独立 APK 一致。**不需要卸载、清除数据或重设清理计划。**

## 保留功能与验证边界

保留 3.0.0 的 Root 文件传输修复、清理规则、引擎及高风险确认。本次不改动清理行为。

发布过程仍校验单元/界面测试、lint、核心回归、签名、内置 APK 一致性及 SHA-256，并在 Android 模拟器检查实际正式包启动。模拟器结果不等于 K80 至尊版、一加 15 的真机 GPU 与 Root 服务验收。
