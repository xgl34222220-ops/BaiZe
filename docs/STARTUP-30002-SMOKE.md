# 30002 最终签名 APK 启动检查

30001 的 Room 无参构造器缺失已在 30002 实际 DEX 中修复。签名 APK SHA-256 为 `600ac2de5f33fdf282f222a2c1099ec63f9ed7766482fe56f238e487836a4fc4`。

首次修复验证运行 34735111483 的编译、247 项 JVM 测试与 lint 通过，但 Android 安装后发生输入焦点 ANR，虽然已显示首页，仍判为失败且未发布。日志记录 CPU pressure avg10=90.81%、memory pressure avg10=15.22%、Google 首次开机服务集中初始化；尚不能仅凭这些指标断言所有卡顿都来自模拟器。

下一轮仅调整测试主机：模拟器分配 3GB 内存，在启动 App 之前留出 90 秒完成系统首次开机，并记录资源压力。所有 App 设置（包括玻璃与动画）保持默认，平台 ANR 超时不变，不点击系统 Wait，不把有 PID 当成通过。新增显式 ANR 日志断言及失败时的 /data/anr 线程转储留存。

App、构建配置、模块与规则源码不变。通过 git diff、已完成的构建步骤状态、APK 固定 SHA-256 和原始测试 XML，复用同一份签名 APK 与已通过的 JVM/lint 结果；Android 启动测试必须完整重跑。复用不代表首次 Android 验证通过。新结果仍以 passed.json 和正式替换工作流为准。
