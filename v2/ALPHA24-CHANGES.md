# Alpha 24 改动摘要

- 修复“仅扫描后点击一键清理又重新全盘扫描”的核心流程错误。
- 首页接入应用缓存与安全项目两套原生 Root 快照引擎。
- 应用缓存枚举与安全项目扫描并行；共享存储中的空项目、隐藏垃圾和碎片只遍历一次。
- 扫描结果清理只消费 30 分钟有效快照，过期或规则变化时明确提示重新扫描。
- 实际删除前保留白名单、路径、符号链接、挂载点、文件状态和大文件二次校验。
- 清理结果记录实际释放空间、文件、目录、空文件、空目录、碎片、异常和耗时。
